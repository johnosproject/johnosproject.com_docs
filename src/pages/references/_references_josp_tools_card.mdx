import Typography from '@mui/material/Typography';
import Paper from '@mui/material/Paper';

export const paper_style = {
  textAlign: 'center',
  padding: '10px',
  height: '250px',
  maxWidth: '320px',
  overflow: 'scroll',
}

<Paper style={paper_style}>
    <img src={props.logoFullPath} style={{width: '40%', margin: '20px'}}/>
    <Typography variant="button" component="h3" gutterBottom>
      <a href={"/references/" + props.dir + "/"}>{props.name}</a>
    </Typography>
    <Typography variant="caption" component="div" color="text.secondary" gutterBottom style={{}}>
        {props.children}
    </Typography>
</Paper>
